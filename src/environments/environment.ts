// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyCxNtFrI1Xzl54fia8QSL8val0bSGAEowM",
    authDomain: "presupuesto-f6394.firebaseapp.com",
    projectId: "presupuesto-f6394",
    storageBucket: "presupuesto-f6394.appspot.com",
    messagingSenderId: "1070558302401",
    appId: "1:1070558302401:web:5ad22bde10c34c265ac440"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
